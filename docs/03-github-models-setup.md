# Настройка GitHub Models API

## Что такое GitHub Models?

**GitHub Models** — бесплатная платформа от GitHub для тестирования AI-моделей, включая DeepSeek, GPT-4, Llama и другие.

**Преимущества:**
- ✅ Бесплатно для разработчиков с GitHub аккаунтом
- ✅ Работает из России (через GitHub инфраструктуру)
- ✅ OpenAI-совместимый API
- ✅ Нет необходимости в кредитной карте
- ✅ Хороший rate limit для тестирования

**Используемая модель:** `DeepSeek-R1`  
**API endpoint:** `https://models.inference.ai.azure.com`

## Шаг 1: Получение GitHub Token

### 1.1. Создание Personal Access Token

1. Перейдите на [github.com/settings/tokens](https://github.com/settings/tokens)
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните форму:
   - **Note:** `Samoyed Bot - DeepSeek API`
   - **Expiration:** `No expiration` (или выберите срок)
   - **Scopes:** оставьте пустым или выберите `read:user` (минимальные права)

4. Нажмите **"Generate token"**
5. **ВАЖНО:** Скопируйте токен — он больше не отобразится!

Токен выглядит так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### 1.2. Альтернатива: Fine-grained token

Если хотите более безопасный токен:
1. [github.com/settings/tokens?type=beta](https://github.com/settings/tokens?type=beta)
2. **"Generate new token"**
3. **Repository access:** `Public Repositories (read-only)` или `All repositories`
4. **Permissions:** оставьте минимальные (не нужны никакие)
5. Создайте токен

## Шаг 2: Настройка проекта

### 2.1. Создание .env файла

Создайте файл `.env` в корне проекта:

```bash
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_telegram_bot_token_here

# GitHub Personal Access Token
GITHUB_TOKEN=ghp_ваш_токен_здесь
```

### 2.2. Проверка .env.example

Убедитесь, что `.env.example` содержит шаблон:

```bash
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_telegram_bot_token_here

# GitHub Personal Access Token (с доступом к Models)
# Получить: github.com/settings/tokens → Generate new token
GITHUB_TOKEN=your_github_token_here
```

### 2.3. Проверка config.js

Файл `config.js` должен содержать:

```javascript
export const config = {
  botToken: process.env.BOT_TOKEN,
  githubToken: process.env.GITHUB_TOKEN,
  databasePath: process.env.DATABASE_PATH || './data/bot.db',
  morningTipTime: { hour: 9, minute: 0 }
};
```

## Шаг 3: Установка зависимостей

```powershell
npm install
```

Убедитесь, что `package.json` содержит:

```json
{
  "dependencies": {
    "dotenv": "^16.4.0",
    "node-schedule": "^2.1.1",
    "openai": "^4.70.0",
    "sql.js": "^1.11.0",
    "telegraf": "^4.16.0"
  }
}
```

## Шаг 4: Проверка интеграции

### 4.1. Код в deepseek.js

Убедитесь, что `src/deepseek.js` содержит:

```javascript
import OpenAI from 'openai';
import { config } from '../config.js';

const client = new OpenAI({
  baseURL: 'https://models.inference.ai.azure.com',
  apiKey: config.githubToken
});
```

### 4.2. Тестовый запуск

```powershell
npm start
```

**Ожидаемый вывод:**
```
✅ База данных инициализирована
📅 Утренний совет запланирован на 9:00 (Europe/Moscow)
🐕 Samoyed Mentor Bot запущен!
```

### 4.3. Тестирование AI

1. Откройте бота в Telegram
2. Нажмите **🤖 Спросить AI**
3. Напишите вопрос: "Привет!"
4. AI должен ответить

## Шаг 5: Диагностика проблем

### Ошибка: "401 Unauthorized"

**Причина:** Неверный GitHub Token

**Решение:**
1. Проверьте `.env` — токен скопирован полностью?
2. Создайте новый токен
3. Перезапустите бота

### Ошибка: "429 Too Many Requests"

**Причина:** Превышен rate limit

**Решение:**
1. GitHub Models имеет лимиты для бесплатного использования
2. Подождите 1-5 минут
3. Для production — рассмотрите платный API

### Ошибка: "Model not found"

**Причина:** Модель недоступна или изменилось имя

**Решение:**
1. Проверьте актуальные модели на [github.com/marketplace/models](https://github.com/marketplace/models)
2. Измените имя модели в `src/deepseek.js`:
```javascript
model: 'DeepSeek-R1' // или другое имя
```

### AI возвращает `<think>...</think>` теги

**Причина:** DeepSeek-R1 показывает процесс мышления

**Решение:** Уже реализовано в коде — теги автоматически удаляются:
```javascript
answer = answer.replace(/<think>[\s\S]*?<\/think>/gi, '').trim();
```

## Лимиты GitHub Models (бесплатный тир)

**DeepSeek-R1:**
- Запросов в минуту: ~15
- Запросов в день: ~500-1000 (неофициально)
- Токенов на запрос: до 4096 входных, 2048 выходных

**Рекомендации:**
- Для тестирования и MVP — достаточно
- Для production с >100 пользователей — рассмотрите платный API

## Альтернативные модели

Если DeepSeek не подходит, попробуйте:

```javascript
// В src/deepseek.js замените model:
model: 'gpt-4o-mini'           // OpenAI (если доступно)
model: 'Meta-Llama-3.1-8B'     // Llama (быстрее, но менее умная)
model: 'Mistral-large'         // Mistral AI
```

**ВАЖНО:** Для других моделей может понадобиться другой токен или API endpoint.

## Безопасность

### ❌ НЕ коммитить в Git
```gitignore
.env
.env.local
.env.production
```

### ✅ Использовать .env.example
Шаблон для других разработчиков — **БЕЗ** реальных токенов.

### 🔒 Ротация токенов
Меняйте GitHub Token каждые 3-6 месяцев или при компрометации.

## Полезные ссылки

- [GitHub Models Marketplace](https://github.com/marketplace/models)
- [GitHub Tokens Settings](https://github.com/settings/tokens)
- [OpenAI SDK Documentation](https://github.com/openai/openai-node)
- [DeepSeek Official](https://www.deepseek.com/)

## Что делать, если GitHub Models перестанет работать?

### План Б: Прямой DeepSeek API

1. Зарегистрируйтесь на [platform.deepseek.com](https://platform.deepseek.com)
2. Получите API ключ (бесплатно ~$5 кредитов)
3. Измените `src/deepseek.js`:

```javascript
const client = new OpenAI({
  baseURL: 'https://api.deepseek.com',
  apiKey: process.env.DEEPSEEK_API_KEY // вместо GITHUB_TOKEN
});
```

### План В: Другие бесплатные API

- **GigaChat** (Сбер) — работает в РФ, бесплатный тир
- **YandexGPT** — работает в РФ, платный
- **Ollama** (локально) — полностью бесплатно, но нужен мощный ПК
