# Технологический стек

## Backend

- **Node.js** v18+ — серверная платформа
- **Telegraf** v4.16+ — фреймворк для Telegram Bot API
- **sql.js** v1.11+ — SQLite в памяти (без нативной компиляции)

## AI / ML

- **gpt-4o-mini** через GitHub Models — генеративная модель (высокие лимиты)
- **OpenAI SDK** v4.70+ — клиент для GitHub Models API

## Утилиты

- **node-schedule** v2.1+ — планировщик cron-задач
- **dotenv** v16.4+ — управление переменными окружения

---

## Почему эти технологии?

### Telegraf

- **Декларативный API** — простые обработчики через `.action()`, `.on()`
- **Middleware поддержка** — хотя мы её не используем (следуем принципу простоты)
- **Активная поддержка** — регулярные обновления, большое сообщество
- **Типизация** — отличная TypeScript поддержка (хотя проект на JS)

### sql.js

**Выбор:** После неудачной попытки миграции на `better-sqlite3` (ошибки компиляции нативных модулей в Windows), вернулись к `sql.js`.

**Плюсы:**
- Работает везде (браузер, Node.js, любая ОС)
- Нет зависимости от компилятора C++ и `node-gyp`
- Простая установка — `npm install` без дополнительных шагов

**Минусы:**
- Синхронная работа с БД  
- Весь файл БД загружается в память
- Ручное сохранение (`saveDatabase()` после каждого изменения)

**Оптимизация:**  
Внедрён **debounce** для записи на диск — изменения накапливаются в памяти и сохраняются раз в 5 секунд, что значительно снижает нагрузку на I/O.

**Когда менять:**  
Если количество пользователей превысит 1000 или возникнут проблемы с производительностью — мигрировать на PostgreSQL.

### gpt-4o-mini через GitHub Models

**Выбор:** Модель заменена с DeepSeek-R1 на gpt-4o-mini из-за жёстких лимитов DeepSeek (8 запросов/день).

**Плюсы:**
- **Высокие лимиты** — ~150 RPM на бесплатном тарифе
- **Быстрые ответы** — меньше задержка по сравнению с полной GPT-4
- **Качество** — достаточно для экспертных ответов по собакам
- **OpenAI-совместимый API** — используем стандартный SDK

**Ограничения:**
- GitHub Models — **бесплатный тариф** для разработки/тестирования
- Для продакшена потребуется платный API

**Fallback:**  
При исчерпании лимитов (429 ошибка) бот возвращает один из 9 заранее заготовленных статических советов, чтобы пользователь не увидел ошибку.

### node-schedule

**Плюсы:**
- Простой cron-подобный синтаксис
- Лёгкий (нет внешних зависимостей типа Redis)
- Достаточно для одного бота

**Минусы:**
- Работает в памяти — при перезапуске бота задачи пересоздаются
- Не подходит для кластерной архитектуры

**Когда менять:**  
Если потребуется горизонтальное масштабирование — использовать Bull (очередь на Redis).

---

## Архитектурные решения

### 1. Stateless бот

Бот не хранит состояние между сообщениями в памяти. Вся логика работает через:
- Callback queries (нажатия кнопок)
- Прямые текстовые сообщения  
- Map `userAiMode` для временного хранения режима AI (сбрасывается после ответа)

**Преимущества:**
- Простота кода
- Легко перезапускать бота
- Не нужна сессионная БД (Redis)

### 2. Контекстный AI

**Проблема:** При прямом доступе к AI (любой текст → запрос) расходовались запросы на случайные сообщения типа "ок", "спасибо".

**Решение:**  
AI работает **только** после активации через кнопку:
- **"🤖 Спросить AI"** → режим `'normal'` (развёрнутые ответы)
- **SOS → "✍️ Написать свою проблему"** → режим `'emergency'` (краткие, срочные советы)

Режим хранится в `Map<userId, 'normal' | 'emergency'>` и сбрасывается после ответа.

**Параметры AI в зависимости от режима:**
- **Normal:** max_tokens=500, temperature=0.7
- **Emergency:** max_tokens=300, temperature=0.5 + специальный промпт для краткости

### 3. Оптимизация производительности

#### Debounce сохранения БД
Раньше: каждое изменение → запись на диск (может быть 10+ записей в секунду).  
Сейчас: изменения накапливаются в памяти → запись раз в 5 секунд через `setTimeout`.

```javascript
let saveTimeout = null;
function scheduleSave() {
  if (saveTimeout) clearTimeout(saveTimeout);
  saveTimeout = setTimeout(() => saveDatabase(), 5000);
}
```

#### Кеширование меню
Раньше: каждый вызов `getMainMenu()` создавал новый объект `Markup.inlineKeyboard`.  
Сейчас: меню создаются один раз как константы (`MAIN_MENU`, `MENU_BUTTON`, `BACK_BUTTON`).

#### Компактная кнопка меню
После ответа AI показывается **одна** кнопка "📋 Меню" вместо 5 кнопок главного меню. Экономит место и делает интерфейс чище.

### 4. Fail-safe механизмы

- **Graceful shutdown** — при SIGINT/SIGTERM бот принудительно сохраняет БД
- **Rate limit handling** — при 429 ошибке возвращается fallback-совет
- **Игнорирование устаревших callback** — не падает при перезапуске бота

---

## Зависимости

Минималистичный стек — **всего 4 основных зависимости**:

```json
{
  "telegraf": "^4.16.3",
  "sql.js": "^1.11.0", 
  "openai": "^4.70.4",
  "node-schedule": "^2.1.1",
  "dotenv": "^16.4.7"
}
```

### Почему так мало?

- **Нет ORM** — прямые SQL-запросы через sql.js
- **Нет Express/Fastify** — работаем только с Telegram, нет HTTP-сервера
- **Нет логгера** — используем `console.log` (для простоты)
- **Нет тестов** — пока проект маленький, тестируем вручную

---

## Потенциальные улучшения

### Производительность

1. **Миграция на PostgreSQL** (при >1000 пользователей)
2. **Redis для сессий** (если понадобится состояние между сообщениями)
3. **Bull для очередей** (если планируется кластер ботов)

### Функциональность

1. **Webhooks вместо long polling** — меньше нагрузка, быстрее ответы
2. **Docker** — упрощение деплоя
3. **PM2** — автоперезапуск при падении
4. **Мониторинг** — Sentry для отлова ошибок

### Разработка

1. **TypeScript** — безопасность типов
2. **ESLint + Prettier** — единый стиль кода
3. **Тесты (Jest)** — автоматическая проверка логики

---

## Итого

Стек выбран с приоритетом **простоты** и **скорости разработки**:
- Минимум зависимостей
- Нет избыточной абстракции
- Легко понять, легко модифицировать

Масштабироваться будем по мере роста нагрузки.
